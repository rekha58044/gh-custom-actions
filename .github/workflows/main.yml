name: main workflow
on: workflow_call
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: checkout code
      uses: actions/checkout@v4
    - name: node Js 
      uses: actions/setup-node@v4
      with:
        node-version: 18.18